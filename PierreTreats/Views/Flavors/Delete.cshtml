@{
  Layout = "_Layout";
}

@model PierresTreat.Models.Flavor

<h2>Are you sure you want to delete this flavor?</h2>

@Html.DisplayNameFor(model => model.Word): @Html.DisplayFor(model => model.Word)
@using (Html.BeginForm())
{
    <input type="submit" value="Delete" />
}
@Html.ActionLink("Back to Tags", "Index")